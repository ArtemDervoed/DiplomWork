// import { fromJS } from 'immutable';
// import { makeSelectRegistrationDomain } from '../selectors';

// const selector = makeSelectRegistrationDomain();

describe('makeSelectRegistrationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
